# 微服务通用配置
SPRING_PROFILES_ACTIVE=prod
NACOS_SERVER=nacos:8848

# Redis 配置
REDIS_PASSWORD=Redis2026SecureKey

# 阿里云 DashScope（Spring AI 兼容 OpenAI 模式）API Key
# 启动校验要求非空；填入真实 key 后 AI 推理功能才可用。占位值仅用于让服务正常启动。
DASHSCOPE_API_KEY=sk-placeholder-set-your-real-key