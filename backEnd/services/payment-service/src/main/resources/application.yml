spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration
  application:
    name: payment-service
  profiles:
    active: dev

server:
  port: 8005