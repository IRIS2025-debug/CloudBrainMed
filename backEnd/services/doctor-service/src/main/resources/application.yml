# backend/services/doctor-service/src/main/resources/application.yml
spring:
  application:
    name: doctor-service
  profiles:
    active: dev
  config:
    import: optional:nacos:doctor-service.yml
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

server:
  port: 8003

internal:
  service-key: ${INTERNAL_SERVICE_KEY:}
